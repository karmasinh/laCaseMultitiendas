# URL del backend (API REST). En desarrollo local se deja vacío y Vite usa el proxy a :3000.
VITE_API_URL=

# URL del backend para Socket.IO (chat, subastas, notificaciones).
VITE_SOCKET_URL=http://localhost:3000
